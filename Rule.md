# Rule.md — 科研流水线硬性规则（不可跳过）

> 本文件是整条科研流水线的"宪法"。任何 Stage、任何 Sub-Agent、任何产出在进入下一环前都必须通过本规则。
> 设计原则（取自 nature-skills / academic-research-skills / OpenAI4S）：显式胜过隐式、一手来源优先、产物可验证、人在回路。
> 违反任意一条 = 退回重做，零容忍。

---

## R0 学术诚信总纲（最高优先级）

- **规则**：严禁编造数据、实验结果、统计量或参考文献。一切结论必须可追溯到真实执行记录。
- **理由**：编造不仅学术不端，且早晚翻车；参考文献造假在 Nature / Science 等期刊已有多次撤稿先例，且会永久损害个人学术信誉。
- **违规后果**：立即终止该 Stage，标记 `INTEGRITY_VIOLATION`，原产物作废，必须人工介入。

## R1 溯源日志（Provenance Logging）

- **规则**：每次外部调用（网络搜索 / API / 数据库 / 模型推理）必须写入 provenance 日志，至少包含：①ISO 时间戳 ②真实请求（原始 query 或 prompt 全文）③返回结果摘要或落盘路径 ④来源 URL / DOI / 调用 ID。
- **理由**：对应 OpenAI4S 的 Action Ledger。没有溯源，结果无法复现、无法审计、无法应对审稿人质疑。
- **格式**：每次调用向 `provenance.log`（JSONL）追加一行。失败 / 超时 / 限流的调用标 `"status":"null"`，**不得补造成功结果**，也不得用"模型推断的合理值"替代。
- **违规后果**：产出缺少对应 provenance → 视为不可信，退回补录。

## R2 引用真实性（No Fabricated Citations）

- **规则**：所有引用必须来自（a）`references/` 中已入库的 bibtex / Markdown，或（b）联网验证真实存在的文献。凡无法确认的一律标 `[CITATION NEEDED]`，禁止写死作者 / 年份 / 卷期 / 页码。
- **理由**：幻觉引用是 AI 写稿最高频的失败模式（Zhao et al. 审计显示普遍存在），且最易被审稿人识破。
- **违规后果**：出现任何无法核验的引用 → 退回，全部改为 `[CITATION NEEDED]` 并补检索；二次违规按 R0 处理。

## R3 只读评审（Read-Only Review）

- **规则**：质量把关 Agent（quality-gate）只评审、不改稿。所有意见写入 review report，绝不修改原稿文件。
- **理由**：评审者改稿会污染作者责任链，且易引入作者未知晓的改动，破坏"作者为负总责"的底线。
- **违规后果**：评审 Agent 若改动原稿 → 退回，本次评审意见作废。

## R4 阶段门禁（Stage Gate）

- **规则**：每个 Stage 产出必须经过 Review（自动或人工）且满足 Handoff Envelope 要求，才能进入下一 Stage。Handoff Envelope 必须包含：上游产物路径、关键结论、未决项、人工审批标记（见 `docs/STAGE-CONTRACTS.md`）。
- **理由**：对应 academic-research-skills 的完整性门禁（Stage 2.5 / 4.5 强制）；防止半成品流入下游放大错误。
- **违规后果**：缺 Handoff Envelope 或门禁未过 → 下游 Stage 拒绝启动。

## R5 反剧场 / 产物契约（No-Output-No-Seat）

- **规则**：每个 Sub-Agent 必须有具体输出文件 + 机器可验证 + 明确的下游消费者。无产物不推进。
- **理由**：无产物不设席位；避免"我设计好了"但没文件，DAG 不前进。
- **违规后果**：只给旁白无文件 → 退回，要求落盘具体产物。

## R6 人在回路（Human-in-the-Loop）

- **规则**：半自动模式下，以下三处必须人工确认才放行：①实验设计方案 ②论文定稿 ③质量把关结论。其余 Stage 自动串联，异常才停。
- **理由**：用户明确选择半自动；这三处是学术风险最高的决策点，需要研究者本人的领域判断。
- **违规后果**：未经人工确认强推这三处 → 视为违规，回退到待确认状态。

## R7 真实执行环境（Real Execution）

- **规则**：代码执行必须在真实环境跑。路径优先级：①本地 Bash 跑 Python（API key 在环境变量）②生成任务文件交远程 GPU 服务器执行 ③生成详细执行说明人工执行。禁止让模型"想象"结果。
- **理由**：直接对应 R0。模型臆测的结果不可信，且 GPU 实验无法在本地沙箱完成时必须显式外溢。
- **违规后果**：出现非真实执行的"结果" → 按 R0 处理。

## R8 范围锁定（Scope Lock）

- **规则**：严格按 Spec / 本协议定义的功能执行，不新增未约定的能力。新增需求走变更流程（记录到 `decisions/OPEN-DECISIONS.md`）。
- **理由**：防止范围蔓延，保持流水线可控、可复现。
- **违规后果**：擅自加功能 → 退回，仅保留约定范围。

## R9 知识库优先（KB-First）

- **规则**：调研与写作优先读取 `references/` 中已存论文 Markdown 与 bibtex 库，保证跨 Stage 一致性；新文献入库后方可被引用。
- **理由**：避免每次重新搜索造成口径不一致，也便于审计引用来源（呼应 R2）。
- **违规后果**：绕过知识库随意引用 → 退回，统一从库中取。

## R10 偏差上报（Speak-Up）

- **规则**：任何 Stage 发现数据异常、结论矛盾、方法不可行，必须显式写入产物并上报，不得"沉默通过"。
- **理由**：沉默放行的错误会在下游被放大，且事后难以定位根因。
- **违规后果**：隐瞒偏差 → 退回，要求显式标注。

---

## 执行约定

- **工作目录**：`RESEARCH_WORKDIR`（默认 `./research-output/`），所有产物落盘于此，目录结构见 `docs/STAGE-CONTRACTS.md`。
- **溯源文件**：`$RESEARCH_WORKDIR/provenance.log`（JSONL，逐行追加）。
- **未决项登记**：`$RESEARCH_WORKDIR/decisions/OPEN-DECISIONS.md`。
- **版本**：`Rule.md v1.0.0`；任何变更须追加到文末"变更记录"，并在 Handoff Envelope 中标注。

## 变更记录

| 日期 | 变更 | 原因 | 影响 |
|------|------|------|------|
| 2026-08-20 | v1.0.0 初版 | 落地科研流水线硬规则骨架 | 全部 Stage 强制执行 |
